# URL of your running Next.js application
NEXT_APP_URL=http://localhost:3000

# Must match MCP_API_SECRET in your Next.js .env file
MCP_API_SECRET=your-secret-here

# Bearer token required by HTTP/SSE clients (Claude.ai remote MCP).
# Can be the same value as MCP_API_SECRET or a separate one.
MCP_HTTP_SECRET=your-http-secret-here

# Transport mode: "stdio" (Claude Desktop) or "http" (Claude.ai remote)
# MCP_TRANSPORT=stdio

# Port for HTTP mode (default: 3100)
# MCP_PORT=3100

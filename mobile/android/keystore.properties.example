# Copy to keystore.properties (gitignored) and fill in your Play upload key details.
# Download the keystore from Expo if needed:
#   cd mobile && npx eas-cli credentials -p android
#
# storeFile is relative to mobile/android/ unless absolute.
storeFile=../@rushabh2310__aagam-holidays.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=YOUR_KEY_ALIAS
keyPassword=YOUR_KEY_PASSWORD

const fs=require('fs');const p='src/components/tour-package-query/QueryVariantsTab.tsx';const s=fs.readFileSync(p,'utf8').split('\n');let pc=0,bc=0,cc=0,ok=true;for(let i=0;i<s.length;i++){const line=s[i];for(let ch of line){if(ch==='(')pc++;if(ch===')')pc--;if(ch==='{')bc++;if(ch==='}')bc--;if(ch==='[')cc++;if(ch===']')cc--;if(pc<0||bc<0||cc<0){console.error('Mismatch at line',i+1,'char',ch);ok=false;break;} } if(!ok)break;} if(ok)console.log('Balances at end: ( )',pc,'{ }',bc,'[ ]',cc);else process.exit(1);
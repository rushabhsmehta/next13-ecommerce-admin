const fs=require('fs');const p='src/components/tour-package-query/QueryVariantsTab.tsx';const s=fs.readFileSync(p,'utf8').split('\n');let pc=0,bc=0,cc=0;for(let i=0;i<s.length;i++){const line=s[i];for(let ch of line){if(ch==='(')pc++;if(ch===')')pc--;if(ch==='{')bc++;if(ch==='}')bc--;if(ch==='[')cc++;if(ch===']')cc--;} if(i % 50 === 0) console.log(`line ${i+1}: ( ) ${pc} { } ${bc} [ ] ${cc}`);}console.log('FINAL',`( ) ${pc} { } ${bc} [ ] ${cc}`);
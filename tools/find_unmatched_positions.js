const fs=require('fs');const p='src/components/tour-package-query/QueryVariantsTab.tsx';const s=fs.readFileSync(p,'utf8').split('\n');function findUnmatched(openChar, closeChar){const stack=[];for(let i=0;i<s.length;i++){const line=s[i];for(let j=0;j<line.length;j++){const ch=line[j];if(ch===openChar)stack.push({line:i+1,col:j+1});else if(ch===closeChar){if(stack.length>0)stack.pop();else return {unmatchedClose:{line:i+1,col:j+1}};} } } return {unmatchedOpen:stack};}
const pRes=findUnmatched('(',')');const bRes=findUnmatched('{','}');console.log('paren result:',pRes);console.log('brace result:',bRes);